# Config files

`Addivox-local.xcconfig` belongs at `../Addivox/Addivox/config/Addivox-local.xcconfig`.

It is ignored in the public repo because it contains local Apple signing settings, so this private copy is here as a backup for rebuilding the dev environment.
